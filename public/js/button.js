function messageOwner() {
    // Send a message to the owner using your preferred method (e.g., email, SMS, messaging platform)
    // For example, using a hypothetical messaging API:
    messagingAPI.sendMessage({
      recipient: +27769743248,
      message: 'Booking confirmed! Details: [insert details]'
    });
  }